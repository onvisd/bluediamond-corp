machine:
    node:
        version: 6.9.2
    environment:
        NODE_ENV: testing
test:
    override:
        - node_modules/.bin/eslint .
        - node_modules/.bin/stylelint ./**/*.css
deployment:
    development:
        branch: development
        commands:
        - ssh bluediamond@planetary.io '. ~/.profile && ~/development/redeploy'
    staging:
        branch: staging
        commands:
        - ssh bluediamond@planetary.io '. ~/.profile && ~/staging/redeploy'
    production:
        branch: master
        commands:
        - ssh site@45.55.190.115 '~/redeploy'
        - ssh site@45.55.205.47 '~/redeploy'
