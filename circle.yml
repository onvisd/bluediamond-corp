machine:
    node:
        version: 6.9.2
    environment:
        NODE_ENV: testing
test:
    override:
        - node_modules/.bin/eslint .
        - node_modules/.bin/stylelint ./**/*.css
deployment:
    development:
        branch: development
        commands:
        - ssh bluediamond@planetary.io '~/development/redeploy'
    staging:
        branch: staging
        commands:
        - ssh bluediamond@planetary.io '~/staging/redeploy'
